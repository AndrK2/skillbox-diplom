/* Gallery Select */
const bundleFilter = document.querySelector('#bundle-filter');
const bundleFilterSelection = new Choices (bundleFilter, {
    searchEnabled: false,
    // the following setting is needed so that the list items are displayed as layout, and not alphabetically
    shouldSort: false,   
})







